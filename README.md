# OpenKala

## UI Direction Policy

- Layout direction is fixed to `RTL` for the entire app.
- UI ordering and alignment must not change based on selected language/locale.
- New screens/components should not rely on locale-driven RTL/LTR behavior.
